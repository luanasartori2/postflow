# Defina o secret no Firebase antes do deploy:
# firebase functions:secrets:set ANTHROPIC_API_KEY
#
# Para emular localmente, use:
# firebase functions:secrets:access ANTHROPIC_API_KEY
# ou configure o secret no console.
